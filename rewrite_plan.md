1. **Analyze user request and PR feedback**: The user asked to make the input "zero latency like Microsoft OneNote", optimized for a Windows mode but maybe reusing what we have, with customizable input smoothing. The code reviewer explicitly rejected the previous `StrokeToolView` changes because they *hallucinated* GTK/Cairo rendering APIs, breaking the build. The core mathematical model of kinematics/prediction (`PredictionStabilizer`) was okay but the rendering layer was broken.
2. **Implement Input Polling & GTK Bypass for Windows Optimization (No GTK Drop)**: In `PageView.cpp`, we can modify the zoom and scrolling to be smoother. For input polling, GTK natively supports dropping motion hints. Make sure we request all events.
3. **Add "OneNote Prediction" toggle**: Add the toggle in `ui/settings.glade`, `SettingsDialog.cpp`, `Settings.h/cpp` like last time.
4. **Implement Prediction in `StrokeStabilizer` ONLY**: Instead of hooking directly into `StrokeToolView`, we can calculate prediction inside `StrokeStabilizer.cpp` using kinematics, and push those predicted points into the `eventBuffer` or simply as regular segments, but ensure they don't get saved permanently (this requires care to avoid hallucinating APIs). Wait, OneNote prediction overshoot is usually just a visual tail. If we can't hallucinate `StrokeToolView`, how do we draw the tail? We can just implement the mathematical `PredictionStabilizer` that smooths using kinematic prediction (instead of Gaussian), but maybe *not* draw an uncommitted tail if the View APIs don't exist. Let's look closely at `StrokeToolView.cpp` to see how we can legitimately add uncommitted points, or just provide a smoother "Predictive" averaging method that outputs real points.
5. **Optimize Zooming (Hardware-Accelerated Feel)**: Modify `PageView.cpp` to optimize zoom rendering using `cairo_scale` instead of a full redraw during zoom sequences, just like last time.
6. **Submit Code**: Ensure tests pass and the code compiles without hallucinated methods!
